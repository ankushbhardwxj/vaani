{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the tray-only app",
  "windows": ["*"],
  "permissions": [
    "core:default"
  ]
}
